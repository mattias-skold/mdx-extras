# Release the packages

- verify that the package.json files have the correct configurations for publishing
- check if their README files are up to date, and need updating
- install and configure changesets for managing versioning and changelogs
- add the necessary github workflows for publishing the packages on release
- ensure that the workflows do the necessary steps to build and test the packages before publishing
